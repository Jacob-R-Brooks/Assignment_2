Before doing anything you must have docker installed and some way to run makefiles such as WSL.

Then run the following commands in order: 
make build
make run

when you are done using the streamlit app you can use control+c to end the app

run: make clean to remove the docker container and image.